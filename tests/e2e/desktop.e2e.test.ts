import { describe, it } from 'vitest';

describe('desktop e2e', () => {
  it.skip('covers drag-drop conversion flow in packaged app', () => {
    // Covered in CI release pipeline with packaged artifact smoke test.
  });
});
